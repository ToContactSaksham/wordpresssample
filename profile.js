function initializeNorthstarProfile(){const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav-links');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('[data-tab]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-tab]').forEach(item=>item.setAttribute('aria-selected','false'));document.querySelectorAll('.tab-panel').forEach(panel=>panel.classList.remove('active'));button.setAttribute('aria-selected','true');document.getElementById(button.dataset.tab).classList.add('active')}));const expand=document.querySelector('[data-expand]');expand?.addEventListener('click',()=>{const target=document.getElementById(expand.dataset.expand),open=target.classList.toggle('open');expand.setAttribute('aria-expanded',open);expand.childNodes[0].nodeValue=open?'Show less ':'Read more '});const modal=document.querySelector('.modal');document.querySelector('[data-modal-open]')?.addEventListener('click',()=>modal.hidden=false);document.querySelectorAll('[data-modal-close]').forEach(item=>item.addEventListener('click',()=>modal.hidden=true));document.addEventListener('keydown',event=>{if(event.key==='Escape')modal.hidden=true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initializeNorthstarProfile);else initializeNorthstarProfile();
